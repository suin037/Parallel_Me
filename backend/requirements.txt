fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27
requests>=2.32
python-multipart>=0.0.9
pandas>=2.2
pyarrow>=16.0            # 관측 경력 경로 parquet 로딩
numpy>=1.26
scikit-learn==1.9.0      # joblib 모델은 학습/서빙 버전이 같아야 함
econml>=0.15
lifelines>=0.28
pyreadstat>=1.2
samplics==0.6.0       # KNHANES 층화·집락·가중치 복합표본 표준오차
anthropic>=0.40
joblib>=1.3
kiwipiepy>=0.17        # 일기모듈(2번) 규칙기반 신호(언어지표·슬랭)
cryptography>=42       # 일기·리포트 SQLite 저장 시 Fernet 암호화
# 일기 감정 모델(선택): DIARY_CKPT 지정 시에만 필요
# transformers==4.44.2  "tokenizers<0.20"  torch
# 심리카드 RAG 정본(선택): 미설치 시 psych_retriever 가 카드 JSON 폴백으로 동작
# chromadb>=0.5  sentence-transformers   (+ preprocess/build_psych_cards_db.py 로 벡터DB 빌드)
