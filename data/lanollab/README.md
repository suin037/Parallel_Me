# Parallel Me — 라놀랩(lanollab) 데이터 전처리 산출물

정신건강·수면·삶의 질 관련 **국가승인통계 3종**의 조회 테이블(lookup)과, 스트레스 대처·긍정정서 **심리학 이론 카드**를 만드는 전처리 결과물입니다.

- 담당 범위: KNHANES / CHS / KWCS 3종 + 심리학 이론 2계열
- 최종 산출: RAG 청크 **56개**, lookup 테이블 **4개**(개별 3 + 통합 마스터 1), 이론 카드 **16장**, 교차검증 뷰 1개

---

## 1. 파일 네이밍 규칙

레포 표준(`[용도]_[데이터셋약칭]_[내용]_v버전.확장자`)을 따릅니다. 다만 **`cards_` 접두사는 라놀랩에서 새로 추가**했습니다.

| 접두사 | 용도 | 배포 여부 |
|---|---|---|
| `lookup_` | Layer 1 룰베이스 조회 테이블 (pandas로 직접 읽음) | ✅ 서비스 배포 |
| `rag_` | ChromaDB 적재용 청크 JSON | ✅ 서비스 배포 |
| `cards_` | 분류·조언용 구조화 카드 JSON (신규) | ✅ 서비스 배포 |
| `raw2tidy_` | 중간 산출물 / 검증 뷰 | 보관용 |

> **`cards_`를 왜 새로 만들었나** — 이 카드들은 `용도: ["분류", "조언"]`으로, `일기_신호어_한국어` 배열로 일기 텍스트에서 개념을 감지하고 `조언_방향`으로 프롬프트를 구성하는 **구조화된 룰 데이터**입니다. `rag_`(ChromaDB 적재용 `{id, document, metadata}` 청크)로 납작하게 만들면 신호어 배열이 사라져 기능을 잃습니다. `lookup_`은 pandas 테이블 규약이라 중첩 JSON과 맞지 않습니다. **팀 승인 필요 — 반대 시 `rag_` 변환 또는 다른 접두사로 조정 가능.**

## 2. 데이터셋 목록

| 폴더 | 약칭 | 원출처 | 대상 | 기간 | 지표 |
|---|---|---|---|---|---|
| 질병관리청_국민건강영양조사 | knhanes | 질병관리청 KNHANES 제9기 | 전국민 | 2022+2023+2024 통합 | 5 |
| 질병관리청_지역사회건강조사 | chs | 질병관리청 CHS | 전국민 | 2025 | 5 |
| 산업안전보건연구원_근로환경조사 | kwcs | 안전보건공단 산업안전보건연구원 KWCS 7차 | 취업자(19세 이상) | 2023 | 4 |
| 심리학_이론카드 | coping / positive_emotion | Lazarus & Folkman 1984·1986 / Fredrickson 2004 | — | — | 카드 16장 |

### 지표 값 (`구분유형 = 전체` 기준)

| 지표명 | KNHANES | CHS | KWCS |
|---|---|---|---|
| 스트레스인지율 | 25.7% | 22.4% | — |
| 우울장애유병률 | 4.6% | 3.8% | — |
| 주중평균수면시간 | 6.87시간 | 6.2시간 | — |
| EQ5D_불안우울문제 | 21.7% | 12.5% | — |
| EQ5D_통증문제 | 38.0% | 27.3% | — |
| 업무스트레스 | — | — | 33.0% |
| 불안감유병 | — | — | 3.6% |
| 우울함유병 | — | — | 2.3% |
| 수면장애 | — | — | 8.3% |

### 카드 구성 (16장)

- `cards_coping_v1.json` — 대처방식 8장: 맞섬 / 거리두기 / 자기통제 / 사회적 지지 추구 / 책임 수용 / 회피 / 계획적 문제해결 / 긍정적 재해석
- `cards_positive_emotion_v1.json` — 긍정정서 4장(기쁨·흥미·만족·사랑) + 메커니즘 4장(확장·되돌림·상승 나선·긍정적 의미 찾기)

## 3. 스키마

### lookup_*_indicators_v1.csv

```
지표명, 구분유형, sex, agegroup, 값, 단위, se, n, 출처, 기간
```

- `구분유형` — `전체` | `성별` | `연령대` | `성별×연령대`. 세분 수준을 나타내며, 해당 없는 차원은 빈 값.
- `se`, `n` — 복합표본 설계 기반 표준오차와 관측수. 신뢰구간이 필요하면 `값 ± 1.96×se`.
- 통합 마스터(`lookup_lanollab_master_v1.csv`, 294행)는 세 조사를 위 스키마로 통일해 세로로 쌓은 것입니다. **서비스는 이 마스터 하나만 읽으면 됩니다.**

### rag_*_chunks_v1.json (청크 56개)

팀 표준(dgroup README 3절) 스키마를 그대로 따릅니다. 필수 메타데이터 6개(`indicator`, `source`, `year`, `age_group`, `topic`, `doc_type`)에 더해 `period`, `population` 두 개를 추가로 넣었습니다.

```json
{
  "id": "knhanes_stress_19-29",
  "document": "국민건강영양조사 제9기 (질병관리청) 2022+2023+2024 기준, 19-29세 성인의 스트레스인지율은 30.7%로 전체 평균(25.7%)보다 5%p 더 높다. 평소 스트레스를 '대단히 많이' 또는 '많이' 느낀다고 응답한 비율이다. 같은 연령대에서 남성은 24.5%, 여성은 37.5%다.",
  "metadata": {
    "indicator": "삶의질",
    "source": "국민건강영양조사 제9기 (질병관리청)",
    "year": 2024,
    "age_group": "19-29",
    "topic": "스트레스",
    "doc_type": "통계",
    "period": "2022+2023+2024",
    "population": "19세 이상 성인"
  }
}
```

지표 하나당 최대 4개 청크를 만듭니다.

| id 접미사 | 내용 | `age_group` |
|---|---|---|
| `_by_age` | 전체값 + 전 연령대 나열 | `전체` |
| `_by_sex` | 성별 비교 + 최고 성별×연령대 | `전체` |
| `_19-29` | 해당 연령대 상세 + 성별 | `19-29` |
| `_30-39` | 해당 연령대 상세 + 성별 | `30-39` |

- **`year`는 정수 한 개**라 KNHANES 3개년 통합은 최신 연도(2024)를 넣고, 실제 기간은 `period`와 문장 안에 담았습니다. 연도로 필터링할 때 주의하세요.
- **`성별×연령대` 168행은 청크로 만들지 않았습니다.** 서사 생성에는 너무 잘고, 정확한 수치가 필요하면 lookup을 직접 조회하는 쪽이 맞습니다. RAG는 맥락, lookup은 정확한 값 — 이렇게 역할을 나눴습니다.
- 청크는 `preprocess/build_rag_chunks.py`가 lookup 마스터에서 생성합니다. **손으로 고치지 마세요.** lookup이 갱신되면 다시 돌리면 됩니다.

### cards_*.json

```json
{
  "metadata": { "개념": "...", "이론계열": "...", "출처_이론": "...", "용도": ["분류", "조언"], "주의": "..." },
  "cards": [{
    "id": "coping_01_confrontive",
    "대처방식": "맞섬 (Confrontive coping)",
    "유형": "문제중심 | 정서중심",
    "능동_회피": "능동 | 회피",
    "정의": "...",
    "행동_특징": ["..."],
    "일기_신호어_한국어": ["따졌다", "맞섰다", "..."],
    "효과성": "...",
    "조언_방향": "..."
  }]
}
```

`positive_emotion`은 `cards` 대신 `positive_emotion_cards` + `mechanism_cards` 두 배열을 가집니다. 메커니즘 카드는 필드 구성이 다릅니다(`원리`, `근거`, `라놀랩_활용`).

## 4. 조인 키 — ⚠️ 팀 표준과 불일치

| 키 | 라놀랩 값 | 비고 |
|---|---|---|
| `agegroup` | `19-29`, `30-39`, `40-49`, `50-59`, `60-69`, `70+` | 아래 참조 |
| `sex` | `1`, `2` (원자료 코드 그대로 — 코드북 기준 1=남성, 2=여성) | 라벨 미적용 |
| `기간` | `2025` / `2022+2023+2024` 문자열 | 통합연도라 정수 아님 |

**연령대 표기가 세 갈래로 갈라져 있습니다.** 조인 전 반드시 매핑이 필요합니다:

- 팀 표준(dgroup README 4절): `~29`, `30-39`
- 라놀랩(이 폴더): `19-29`, `30-39`
- dgroup 실제 파일(`lookup_qol_indicators_v2.csv`): `20대`, `30대`, `전체`

`30-39`는 표준과 일치하지만 `19-29` ↔ `~29`는 다릅니다. 라놀랩은 19세 미만을 지표에서 제외했기 때문에(KWCS는 19세 이상 대상) `~29`로 바꾸면 "18세 이하 포함"으로 오독될 수 있어 `19-29`를 유지했습니다. **팀 논의 후 통일 필요.**

⚠️ **이 불일치는 RAG 검색에도 그대로 영향을 줍니다.** `rag_*_chunks_v1.json`의 `metadata.age_group`도 같은 `19-29`를 씁니다. 라놀랩 청크와 dgroup 청크를 같은 ChromaDB에 넣고 `age_group="~29"`로 필터링하면 **라놀랩 청크는 하나도 안 걸립니다.** 적재 전에 반드시 표기를 통일하세요. 값만 바꾸면 되므로 `build_rag_chunks.py`를 다시 돌리면 반영됩니다.

## 5. ⚠️ 사용 시 주의사항 (함정 목록)

1. **KNHANES와 CHS 값을 혼용 금지.** 같은 지표명이라도 값 차이가 큽니다. EQ5D_불안우울문제 21.7% vs 12.5%(9.2%p), EQ5D_통증문제 38.0% vs 27.3%(10.7%p). 조사 도구·조사 방식·기간이 달라서이며, **어느 쪽이 틀린 게 아닙니다.** 차이 전체는 `raw2tidy_lanollab_crosscheck_v1.csv`에 정리돼 있습니다. 하나의 시나리오 안에서는 한 출처만 인용하세요.
2. **KWCS는 모집단이 다릅니다.** 취업자 19세 이상 한정이라 전국민 대상 KNHANES·CHS와 직접 비교 불가.
3. **KWCS 지표는 이름이 비슷해도 다른 문항입니다.** `업무스트레스`(33.0%)는 업무 국한 문항이라 KNHANES·CHS의 `스트레스인지율`(전반적 스트레스)과 같은 지표가 아닙니다. 이름만 보고 같은 축에 놓지 마세요.
4. **모든 값은 복합표본 설계 가중 추정치**입니다. person_level에서 단순 평균을 내면 다른 값이 나옵니다. 재집계 시 반드시 가중치·층화·집락 변수를 함께 쓰세요.
5. **KNHANES는 3개년 통합 추정치**입니다. 단년도 값이 아니며 통합가중치(`wt_pool_*`)를 적용했습니다. 연도별 추이 용도로 쓸 수 없습니다.
6. **일기 신호어는 초안입니다.** 실제 사용자 데이터로 보정 전이며, 감지 성능이 검증되지 않았습니다.
7. **원 척도 문항은 포함하지 않았습니다.** Ways of Coping 척도 문항 자체는 저작물이라 개념만 재구성했습니다. 원문 문항을 카드에 추가하지 마세요.
8. **positivity ratio(긍정:부정 3:1)는 의도적으로 제외**했습니다. 근거가 된 수학모델이 학계에서 반박되었습니다. 팀 논의 없이 추가 금지.
9. **dgroup의 심리학 청크와 중복 가능성.** dgroup에 Ryff 기반 `rag_wellbeing_papers_chunks_v2.json`이 있고 `sohyun` 브랜치에 `preprocess/paper_rag.py`·`psychology_db.pkl`이 있습니다. 라놀랩 카드(Lazarus & Folkman, Fredrickson)와 이론계열은 다르지만 **적재 전 담당자와 범위 조율 필요.**

## 6. 원자료(raw) 정책 — 레포 미포함

원자료와 **개인 단위 정제본 모두** 레포에 올리지 않습니다(`.gitignore` 처리). 팀의 기존 정책(dgroup README 6절)을 그대로 따릅니다.

| 미포함 대상 | 위치 | 출처 |
|---|---|---|
| `chs25_all.sas7bdat` (368MB) | `data/raw/chs/` | 질병관리청 지역사회건강조사 (chs.kdca.go.kr) |
| `HN22_all.sav`, `HN23_all.sav`, `HN24_ALL.sav` (총 361MB) | `data/raw/knhanes/` | 질병관리청 국민건강영양조사 (knhanes.kdca.go.kr) |
| `[데이터] 2023년 제7차 근로환경조사.sav` (33MB) | `data/raw/kwcs/` | 안전보건공단 산업안전보건연구원 (oshri.kosha.or.kr) |
| `*_person_level.csv` (총 23MB) | `data/processed/` | 위 스크립트로 재생성 |

`data/processed/`의 person_level 정제본은 **개인 단위 데이터라 커밋하지 않습니다.** 원자료를 받아 아래 스크립트를 돌리면 그대로 재생성되므로 재현성은 유지됩니다.

## 7. 재현 방법

원자료를 위 표의 위치에 두고, 레포 루트에서 실행:

```bash
python preprocess/knhanes_preprocess.py   # → lookup_knhanes_indicators_v1.csv
python preprocess/chs_preprocess.py       # → lookup_chs_indicators_v1.csv
python preprocess/kwcs_preprocess.py      # → lookup_kwcs_indicators_v1.csv
python preprocess/merge_reference_tables.py   # → lookup_lanollab_master_v1.csv + crosscheck
python preprocess/build_rag_chunks.py         # → rag_*_chunks_v1.json (원자료 불필요)
```

`build_rag_chunks.py`는 lookup 마스터만 읽으므로 **원자료 없이도 실행됩니다.**

의존성은 `backend/requirements.txt`의 `pandas`, `numpy`, `pyreadstat`로 충족됩니다.

## 8. 남은 작업 (TODO)

- [ ] `agegroup` / `metadata.age_group` 표기 팀 표준 통일 (4절) — **ChromaDB 적재 전 필수**
- [ ] lookup 컬럼명 한글/영문 통일 (dgroup은 `indicator_name`·`value`·`unit` 영문, 라놀랩은 혼재)
- [ ] `cards_` 접두사 팀 승인 (1절)
- [ ] dgroup·sohyun 심리학 RAG와 범위 조율 (5절 9번)
- [ ] `sex` 코드 → 라벨(`남성`/`여성`) 매핑 여부 결정
- [ ] 일기 신호어 실사용 데이터 기반 보정
- [ ] 변수 사전(`dict_`) 미작성 — 팀 템플릿 형식 확인 후 추가
